apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yml
- services-best-practice.yml
- secrets.yml
- debug-pod.yml

commonLabels:
  app.kubernetes.io/name: privee
  app.kubernetes.io/managed-by: kustomize

namespace: default

# Additional metadata for all resources
commonAnnotations:
  description: "Privee application deployment on AKS"
  version: "1.0.0"

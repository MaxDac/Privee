apiVersion: v1
kind: Secret
metadata:
  name: grafana-otel-secret
  namespace: default
type: Opaque
data:
  # Base64 encoded Grafana OTEL token
  # Replace this with: echo -n "YOUR_GRAFANA_TOKEN" | base64
  GRAFANA_OTEL_TOKEN: REPLACE_WITH_BASE64_ENCODED_TOKEN
